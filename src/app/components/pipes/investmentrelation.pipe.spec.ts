import { InvestmentrelationPipe } from './investmentrelation.pipe';

describe('InvestmentrelationPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentrelationPipe();
    expect(pipe).toBeTruthy();
  });
});
